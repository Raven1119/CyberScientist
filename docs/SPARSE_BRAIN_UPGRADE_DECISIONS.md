# CS-SB-01 稀疏监督大脑决策增量（2026-09-24）

| 事实 | 最小修改 | 验证边界 |
|---|---|---|
| 旧 `executor_question` 把 `answers` 限为表单枚举，且理由在旧通路被截断；选项会限制研究判断 | 新 Run 以 `ResearchAnswer.answer_md` 为主，`native_answers` 只在合法可映射时使用；原生无法表达时合法 decline/cancel，并用现有指导队列在自然边界投递全文和 ACK。旧 Run 保留原协议。 | fake runtime 闭环验证零读取、第三路线、全文投递及 ACK；真实 Kimi/Codex 本轮未调用。 |
| 默认帧含执行器进展摘录，旧周期兜底会按普通活动唤醒大脑；不适合长期稀疏研究判断 | 新 Run 默认高层状态，只有研究级事件/显式请求唤醒，时间巡检不调用模型；`research_trace` 只在大脑主动 list/read 时查本 Run 已保存的公开记录。独立判断为核心，轨迹按需读取，未增加主动核查流程。 | 临时 SQLite + fake runtime 测试验证长 Job/普通进度不唤醒、关键检查点唤醒、读取数可为零。 |
| 新只读令牌若沿用 `_prime_spec` 全 Run revoke 会在执行器会话重建时失效 | 令牌按角色撤销；脑 token 仅允许 `research_trace`，执行器 token 保留既有写工具；Run 终止撤销全部。 | HTTP 鉴权测试覆盖错角色、其他 Run、未来序号、完成审阅、版本不符的冻结清单。 |
| 原生代理内建工具没有经本轮真实协议验证的完整 allowlist | 项目 MCP 桥与后端权限隔离，Codex 大脑线程使用只读沙箱；提示禁用通用 Shell，但不能声称两个原生运行时的所有内建只读/网络工具已被硬禁用。 | 本轮禁止额外真实模型探针；这一能力边界留作后续有界验证。 |
