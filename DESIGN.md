# CyberScientist：独立思考、稀疏监督与按需轨迹读取

状态：待实现的设计，不代表代码已修改或真实科研收益已验证。
核对基线：Raven1119/CyberScientist，`9b7fe6735732ec73bf7c908d6cb0b1ae61372b9f`（2026-09-23）。实施以本地实际 HEAD、未提交改动和已安装运行时为准，不回退到此提交。

## 1. 升级目标与最终架构

### 用户已确定的方向

大脑长期保留宏观研究认识，在关键节点稀疏介入；执行器持续自主研究、接触详细工具现场。大脑应有充分的独立判断空间，能够否定提问前提、拒绝二选一、提出第三条路线、暂不判断，也能够在依据充分时同意执行器。

本轮最后一条澄清具有优先级：**重点是独立思考。轨迹由大脑按需读取，不建设主动核查流程，不要求每个问题先读轨迹，不让大脑承担持续审计。**

这与稀疏监督可以同时成立：系统可因研究级事件主动唤醒大脑；唤醒后，是否读取轨迹、读取哪部分、是否介入，交给大脑判断。没有执行器求助，也能发生必要的监督；没有大脑读取请求，也不自动展开工具轨迹。

### 运行结构

```text
执行器：连续自主工作 ──→ 现有事件／检查点／Job 回执
        │                         │
        │ 研究进展或研究问题        │ 只保存，不自动启动核查
        ↓                         │
现有控制器：合并变化、稀疏唤醒       │
        ↓                         │
大脑：长期研究上下文                │
        ├─ 可以直接形成判断         │
        ├─ 需要时请求读取轨迹 ───────┘
        │       ↑ 仅返回选中的有界公开片段
        └─ 开放式判断／方向指导
                    ↓
            现有指导队列 → 执行器
```

“干净上下文”保留目标、关键事实、方法与约束、判断理由、反证和历次方向变化。工具操作流水默认不进入。大脑主动选择的必要原始片段可以进入；不能承诺读取过的内容会自动从原生会话历史消失。

### 不做的事情

不增加顾问、常驻观察 Agent、强制审稿 Agent、双模型辩论或每题两次盲审；不重写原生 Agent 内核，不改为普通聊天 API；不建设研究 DAG、分支搜索或通用验收平台；不扩展科学 Skills，不更换模型或提升默认思考强度。原有 Job 授权、unknown 占位、密钥隔离和提交边界继续保留。

本轮是一条完整升级切片：**稀疏唤醒 → 开放问题 → 可选读取 → 独立判断 → 完整交付 → 执行器继续。**

## 2. 三个升级点：做法、理由与具体边界

### 升级 A：把开放式研究判断与原生选择题回执分开

#### 当前限制与理由

基线的 `_validate_question_answer()` 强制答案覆盖必答项并取自 `options.const`；`extract_question_answer()` 要求非空 `answers`，且只保留该映射和最多 500 字符的 `reason_md`；共享 `_question_prompt()` 明确要求选择现有 const。[S1][S2][S3]

因此，只写“请独立思考”不够。大脑即使想回答“前提不成立，先做另一件事”，也可能被校验拒收或在回传中丢失。需要改变项目内部的答案契约及其交付路径。

#### 改动

项目内部增加开放式 `ResearchAnswer`。名称可按仓库习惯调整，但以下语义不可缺失：

```json
{
  "schema_version": 1,
  "message_type": "research_answer",
  "request_id": "由后端绑定的本次研究问题",
  "answer_md": "完整研究判断：可以选择、补充、否定前提、提出新路线或说明未知。",
  "evidence_refs": [],
  "native_answers": null
}
```

`answer_md` 是主体；不限定为赞成／反对，不要求给出新方案，不强制制造反对意见。`native_answers` 只是可选的原生表单映射，允许为 null。不存在适当原生选项时，研究回答仍是有效回答。`evidence_refs=[]` 可以合法存在，例如回答是对问题逻辑的分析；引用非空时必须能解析到实际可用来源，不能虚构。

执行器原问题及其选项保留为原始材料。选项是执行器提供的建议集合，不作为研究答案的枚举边界；问题中的“推荐选项”“显然正确”等措辞不赋予额外权威。大脑输入同时保留用户目标、约束及必要的现有研究状态，不只给它一份选择题。

不隐藏原问题、不用预处理器替大脑作语义改写、不安排强制第二轮“反对者审查”。提示词只说明新协议与角色边界；核心变化是自由答案能够被接收、保存并实际送达。

#### 一个有用的例子

执行器问：“频率失败了，是否把内存加倍？是／否。”

大脑可以回答：“当前资料不足以把失败归因于内存。保留成功几何，先明确失败阶段；独立产物可以继续。现在不选择加倍或维持。”

也可以直接回答“是”，并说明充分依据。验收不能奖励反对次数；独立判断允许合理同意。

#### 原生接口仍然遵守原生协议

原生 MCP elicitation 的 `accept.content` 需要符合请求中的 schema；不能把自由文字硬塞进 boolean 或 enum。该约束只保留在适配器的最后一层。[S4]

| 原生请求与研究回答 | 处理方式 |
|---|---|
| 大脑明确提供了符合原 schema 的 `native_answers` | 原生接口可以合法 accept；完整 `answer_md` 仍须通过正文交付，不能只剩一个选项。 |
| 大脑未选择任何原选项，或回答无法无损表示 | 先保存完整研究判断；原生表单按已核实协议 decline／cancel，随后通过现有指导队列在可交付边界传递正文。不能随意选“最接近”的选项。 |
| 原生调用已超时、Run 已暂停或答案已经过期 | 保留真实请求状态；不能补发伪造的旧回执，不能让迟到答案扩大权限或作用于另一 Trial。 |

“原生表单没有接受”与“研究问题没有得到回答”分开记录。自由答案已成立时，UI 与控制器不能把它误报为脑回答失败。

新研究问题优先沿用共享的 `research_checkpoint` 路径，以一个可选问题字段承载问题和参考选项，复用 `review=async/blocking`、review ID 和指导队列。该 MCP 调用应迅速返回“已登记／继续或让出”；不要让工具连接同步等待长时间模型推理。原生 AskUserQuestion 保留兼容适配，不能成为唯一通道。

原生问答的正文交付必须形成真实闭环：答案保存 → 合法原生回执 → 既有自然边界投递 → 执行器取得正文并继续。不能只在数据库或前端展示答案；不能出现“等 ACK 才投递、等投递才 ACK”的循环。必要时使用既有 blocking/yield 边界，但普通已授权读取和落盘不因一次异步研究问题全部停止。

#### 安全边界

研究选项可以开放，权限边界不开放。付费额度、密钥、正式提交或权限升级仍由控制器与用户授权。大脑不能把“另一个方案”变成新增未授权操作。普通局部工具问题不再无差别进入大脑会话；研究问题进入开放协议，明确的权限问题进入原有授权路径。

### 升级 B：轨迹可由大脑选读，不自动展开，不建设核查流水线

#### 当前限制与理由

基线 `observation.py` 会把一组工具片段和结果摘录默认送入观测帧；Kimi 大脑的 `session/new` 则把 `mcpServers` 固定为空。[S5][S3] 用户需要的是大脑在必要时能摆脱执行器的选择性叙述，又不长期堆积工具过程。

因此保留现有记录，增加一个窄的只读入口；撤销“每次由系统主动核查轨迹”的设想。

#### 默认输入

大脑默认收到：研究目标及约束、关键进展／未知、执行器提出的问题、后端已有的简短任务／评分状态，以及本轮轨迹入口。科学叙述标记为执行器报告，平台状态标记为实际回执；证据不足不升级为已验证。

新增研究摘要字段可承载高层变化。完整 `report_md` 和公共工具记录继续保存，但不能作为摘要缺失时的自动整段回灌。已有字段足够时直接复用，避免建立第二份全量研究数据库。

#### 单一只读能力：`research_trace`

复用现有 MCP 桥，增加一个仅对大脑开放的读取工具，支持 `list` 和 `read` 两种动作。它的工作只是在已有记录里定位并返回内容，不调用另一个 LLM、不下载云端新文件、不启动计算。

| 动作 | 输入与行为 |
|---|---|
| `list` | 可选关键字、事件类型与游标；列出当前 Run 可用的公开记录引用。不限于执行器在问题中推荐的引用，也不限于最近一个 Trial。 |
| `read` | 使用实际引用和范围读取已保存的公开内容；返回来源、记录版本或哈希、时间／序号、内容、截断标记和续读位置。 |

第一版覆盖已有的公开事件、检查点、Job 回执、提交／评分回执，以及能可靠解析的已冻结 Job 输入清单。只读取已登记且版本可确定的产物；没有原始日志时明确报告缺失，不把历史被截断的摘录冒充完整日志。不给任意文件路径、Shell、通用网络或隐藏推理轨迹访问能力。

记录由后端确定身份和可见性，不能相信模型自报 run_id。每次审阅开始绑定证据截止序号；后续读取在同一视图内完成，新结果留给下一次审阅，避免读到未来事实却沿用旧决策身份。查询可以搜索整个已授权 Run 的截止前材料，不强迫只读“当前问题引用”。

可以直接复用既有预算方式限制每次返回长度与一轮累计读取量。起始建议是单次最多约 6,000 字符、一轮累计约 24,000 字符，作为可调整的工程上限；不是要求大脑用满额度。读取到上限时返回缺项／续读信息，不自动切成全日志注入，不悄悄追加模型调用。

#### 明确允许“零读取”

大脑可以完全不调用 `research_trace` 就回答。系统不要求提供读取理由，不检查“是否先查过证据才允许回答”，不以读取次数评价独立性，不创建自动取证任务。

确实需要阅读时，内容直接来自后台保存的记录，不让执行器先重新解释。读过的片段作为必要依据进入当前原生会话；除非以后发生明确交接，否则不要声称能从供应商会话历史中删除它。

#### 只读能力的接线边界

大脑保留原生 Kimi/Codex，会话里只开放本轮必要的轨迹读取能力；不开放普通 Shell、安装、调试和任意文件操作。执行器的既有工具能力保持不变。能力限制必须通过已安装版本支持的配置和运行探针核对，隐藏工具事件或写一句“不要使用工具”不能当成权限隔离。

复用现有短时能力令牌，并区分大脑读取角色与执行器角色。特别注意 `_prime_spec()` 目前会撤销整个 Run 的令牌：加入大脑令牌后，执行器重建只应撤销自身角色／代次，不能让大脑读取入口随之失效。Run 结束仍撤销全部令牌。后端验证实际角色和请求绑定，不能只按工具列表隐藏写入口。

### 升级 C：保留稀疏主动监督，隔离长期研究会话

#### 稀疏唤醒

延续已同意的方案：定时器负责健康检查，不因“时间到了＋又有工具活动”直接调用大脑。研究请求、用户目标变化、关键阶段结果和疑似研究停滞可以合并为一次审阅。既有生命周期、watchdog、问答和 shadow 路径都要检查，不能只修改周期 shadow。[S6]

正常长计算、排队和重复查询不制造审阅；相同证据版本的重复故障不反复叫醒大脑。有明显反证或其他独立工作发生重要变化时，不能因为仍有一个 Job 正在运行就全局压制审阅。

不完全依赖执行器主动求助：已有回执显示连续失败、未按工作边界获得可解释进展时，可以触发一次“疑似停滞”审阅。程序只报告可观测症状，不靠字符串规则声称已经理解科学根因；无新证据、正常长任务和通信异常分开处理。必要的健康核对不等于新增轨迹审计流程。

大脑被唤醒后可以静默、指导、调整方向，或自行选读材料。它不必先核查。已有 `start_trial/steer/pause/finish` 的宏观权力保留；普通非阻塞审阅不暂停执行器。

#### 长期会话

同一个长期研究会话保留方向、理由和关键事实，不每次重置。不同问题的研究结论可以积累；不把安装日志、普通工具问答和经验整理素材塞回该会话。开局需要联网调查时，由执行器承担具体操作，大脑提出高层调查目标。

取消大脑对操作技能正文的自动注入；保留研究所需的方法、约束和已经冻结的适用经验。此项只调整上下文投递，不更改现有经验内容、Skills 能力、模型身份或思考强度。

已混入工具材料的旧会话不做静默清洗。新 Run 使用新版；需要恢复旧 Run 时用明确的高层研究交接建立新会话，保留原始历史与版本。显式研究笔记保存判断理由、反证和未知，不读取隐藏思维链。有限的按需证据片段允许进入新会话，不能追求字面上的“零事实”。

#### 理由

TBMA 复盘记录了 36 次审阅、5 条内部指导，同时记录了大脑提供有效恢复建议，以及正常长计算发生无新增证据的审阅。[S7] 这些支持改进调用与上下文分配，但不能证明“调用越少越强”或“大脑应该被移除”。

用户希望保留跨尝试的宏观视角。此次升级应让它获得独立选择信息和表达判断的自由，同时减少不相关输入与无意义调用。

## 3. 实施范围、验收与证据

### 一个完整切片

直接在现有后端、原生适配器、MCP 桥、指导队列和研究页上实施。新增数据优先放入既有 review/frame/result JSON；只在必需时追加最少字段。不要预建新索引服务、图数据库、调度器或多 Agent 平台。

| 位置 | 本轮改动 |
|---|---|
| `controller.py`、`decision_extraction.py` | 开放研究回答；原生映射只在运输层校验；问题正文完整交付；各类唤醒归一。 |
| `observation.py`、`collab.py`、协作 schema | 默认高层输入；问题与回执身份；按需轨迹引用／读取；沿用已有指导与状态机。 |
| `mcp_bridge.py`、`api.py`、`codex_protocol.py` | 单一只读能力、按角色能力令牌与服务端校验。 |
| `brains/codex.py`、`brains/kimi.py`、`prime/kimi_acp.py` 及相关执行器适配 | 原生会话接线、答案解析、合法 native decline/accept、正文投递；不修改上游运行时内核。 |
| 研究页与文档 | 唤醒原因、开放答案、正文是否投递、仅实际发生的读取记录；不重做前端风格。 |

现有材料记录的 Job 门禁、unknown 占位、密钥隔离与过期指导是已有能力，不作为本轮新增成果。[S8]

### 六组验收，覆盖能力链而非堆积测试清单

| 场景 | 必须观察到的行为 |
|---|---|
| **开放判断与合理同意** | 执行器给是／否，大脑能提出第三路线，完整正文到达执行器；也能正常同意。内部自由回答不被选项校验拒绝，不被 500 字符旧截断丢弃。原生 wire response 仍合法。 |
| **按需读取与零读取** | 大脑不读轨迹也能正常回答，读取调用数为零；主动读取时能发现执行器未引用的当前 Run 记录。无自动预读、无必读配额、无强制核查步骤。 |
| **语义与证据来源** | 执行器声称完成，已保存回执却表示失败／未知：所选片段保持原状并标明来源；后端不把自述升为已验证，不替模型先做语义裁决。 |
| **稀疏性与仍能主动唤醒** | 长计算和重复轮询不反复审阅；真实阶段变化／疑似停滞可以在无求助时触发一次审阅。读取和大脑输出不能反过来自激新审阅。 |
| **会话、权限与运输闭环** | 大脑保留长期研究判断，只按需获得少量片段；普通工具问答不绕回该会话。大脑读取令牌不能写 checkpoint／Job，执行器重建不撤销大脑权限。native 表单拒绝与研究回答成功可同时正确展示，队列无悬挂。 |
| **幂等与恢复回归** | 重复请求不重复投递，暂停／结束后的迟到结果不驱动操作；读取不足显式未知；既有授权、unknown、提交、恢复与经验审批不退化。 |

固定假的大脑回答只能验证运输通道，不能证明模型已减少迎合。另保留一组小型、默认不调用模型的评估样例：相同事实使用不同诱导选项，加一个执行器判断确实正确的例子。真实授权后，用同模型、同强度、同预算比较，评价是否基于事实独立判断，而非是否“反对更多”。不预设 TBMA 必须走有限差分或任何指定答案。

### 授权与交付边界

这张任务只授权代码修改、隔离环境内应用测试和构建。不要恢复 TBMA、创建新的付费 Job、提交比赛 Attempt、调用额外付费模型或改动全局认证。用户当前正在用的 Codex 开发会话可以继续实施；它不等于对应用中新开科研会话的授权。

先在临时工作区完成 fake-runtime 闭环与协议级测试。真实运行时能力是否核实、真实模型是否往返、是否开展真实科研，分别记录。能力配置不受当前版本支持时，明确缺项并完成其余可测代码；不能悄悄换模型、扩大权限或把 stub 叫作已接通。

### 来源与依据

下列源码均按上述基线核对。行号可随本地更新变化，实施时按函数定位。

| 标识 | 来源 | 支持的事实 |
|---|---|---|
| S1 | `src/cyberscientist/controller.py::_validate_question_answer`、`_run_question_review` | 内部答案被要求映射执行器选项，同一大脑会话承担问答。 |
| S2 | `src/cyberscientist/decision_extraction.py::extract_question_answer` | 非空 `answers` 要求及理由 500 字符保留限制。 |
| S3 | `src/cyberscientist/brains/kimi.py::_question_prompt/open` | 封闭问答提示和大脑空 MCP 配置；Codex 复用共享问答提示。 |
| S4 | MCP 官方 `2025-11-25/client/elicitation`，Requested Schema、Response Actions | 原生接受内容须匹配 schema，可 decline/cancel；仅用于运输边界参考，实际按安装版本协议核对。 |
| S5 | `src/cyberscientist/observation.py::executor_digest/build_frame` | 默认工具结果摘录投影。 |
| S6 | `controller.py::_maybe_periodic_shadow/_handle_signal/_run_loop` | 周期、watchdog、生命周期和共享会话入口。 |
| S7 | 用户附件 `TBMA_RUN_REVIEW_2026-09-22.md`，“暂停与当前结果”“系统自主做了什么”“建议的修复次序” | 36 次审阅、5 条指导、有效恢复协作、无新增证据的静默审阅；均为历史复盘，未作为本轮效果证明。 |
| S8 | 用户附件 `TBMA_UPGRADE_2026-09-23.md`，“已实现的闭环”“验证与边界” | 已有修复与尚未完成真实计费验证的边界。 |

官方协议参考地址：`https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation`。
原生接入参考地址：`https://developers.openai.com/codex/app-server/`；此处不固定任何未经本机探针核实的新参数。
