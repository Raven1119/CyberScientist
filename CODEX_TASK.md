# CODEX TASK — CS-SB-01：独立思考的稀疏监督大脑

任务性质：直接实施一个最小完整切片，不再输出另一份全面架构计划。
配套设计：同目录 `DESIGN.md`。
核对基线：`Raven1119/CyberScientist@9b7fe6735732ec73bf7c908d6cb0b1ae61372b9f`。先检查实际 HEAD 和工作区，不 checkout/reset 到旧基线，不覆盖用户未提交代码。

## 1. 目标与不可改变的语义

保留当前“大脑＋执行器”：大脑长期维持宏观研究判断并稀疏介入，执行器连续自主研究。沿用原生 Codex/Kimi、现有 SQLite、Job 网关、MCP、检查点、指导队列和前端。

**用户最后补充：重要的是大脑独立思考。轨迹只由大脑按需读取。不要建设主动核查流程，不要求每次回答先读轨迹，不把大脑变成审计员。**

本轮必须跑通：执行器提出问题（可有诱导选项）→ 大脑收到高层研究信息 → 可以零读取直接回答，也可以自行选读当前 Run 公开记录 → 给出不限于选项的完整判断 → 控制器保留并可靠交付 → 执行器继续。没有执行器求助时，研究级事件也能稀疏唤醒大脑。

大脑可以同意、否定问题前提、提出第三条路线或保留未知。不能强制反对、强制核查、强制二次模型审阅。自由研究意见不扩大预算或权限。

不新增常驻 Agent、不改模型及思考强度、不扩展科学 Skills、不建设 DAG／分支搜索／通用验收平台、不重写上游运行时。具体工程命名可按当前代码缩减或调整，不能改变上述语义。

## 2. 施工要求：完成同一条闭环

### 开工定位与授权

在 Linux／WSL2 工作区操作，先读 `AGENTS.md`、`README.md`、当前协作实现和本卡。重点定位下表函数；确认已有升级，避免重复重写。检查本地是否有运行中的科研 Run，不自行暂停、恢复或重启用户服务。测试使用临时数据库和工作目录，不使用生产凭据。

本卡授权应用代码修改、隔离应用测试和前端构建；不授权新科研运行、额外真实模型探针、付费 Job、比赛提交、全局 CLI 配置或认证变更。模型／云端验证没有授权时，完成实现与可执行测试，准确留下未验证项。

### 直接改动清单

| 改动 | 必须实现 | 原因与关键落点 |
|---|---|---|
| **开放研究回答** | 引入或等价实现 `ResearchAnswer(request_id, answer_md, evidence_refs, native_answers?)`；`native_answers` 可为 null；完整答案不能被旧 `answers` 非空要求或 `options.const` 拒绝。 | 改 `decision_extraction.py`、`controller.py` 问答路径及 Kimi/Codex 共享提示。执行器选项仅是参考，不是思考边界。去掉理由 500 字符静默截断。 |
| **共同问题入口与原生适配** | 在已有 checkpoint／review 路径承载研究问题，复用 async/blocking 与指导队列。Kimi 原生 AskUserQuestion 转接同一内部语义；已有原生 Codex 输入路径按实际支持情况对齐，不编造接口。 | 原生 enum 表单仍须合法。自由答案不能映射时，合法 decline/cancel 与研究答案已完成分别登记；在现有自然边界完整投递，不能只存库不交付。 |
| **按需轨迹读取** | 增加单一 `research_trace` MCP，只读 list/read；默认零调用。大脑可自行定位当前 Run 截止前的公开记录，不限于执行器提供的引用。 | 复用 `observation.py`、MCP 桥及后端。仅返回实际已有且可定位的内容，带来源／范围／版本／截断。禁止自动取证、全日志回灌、隐藏推理、任意路径与云端新操作。 |
| **高层输入及会话隔离** | 默认投递研究摘要、关键事实与未知，保留完整原始记录但不自动输入。大脑继续长期研究会话；只有主动选中的少量片段进入。普通工具问答、开局 Shell 探查、维护整理离开该会话。 | 调整 `observation.py` 及生命周期、问答所有 packet；大脑不自动注入操作技能。开放必要只读能力，限制通用执行能力，不能仅隐藏输出或靠提示词声称隔离。 |
| **稀疏唤醒与不中断执行** | 时间驱动健康检查不等于模型审阅；研究级变化、关键结果、显式研究请求和疑似停滞可触发。去重／合并；正常长计算、重复轮询及大脑自身读操作不自激。 | 同时检查 `_maybe_periodic_shadow`、watchdog／`trial.stalled`、生命周期、问答等入口；不能只改一个计时器。保留宏观 start/steer/pause/finish 与自然边界指导。 |
| **权限、前端与历史兼容** | 读工具服务端鉴权；研究问答不越权。UI 显示完整答案、原生表单状态、正文交付状态、唤醒原因与实际读取引用。旧数据可读，新 Run 使用升级；不静默改运行中会话。 | 新增大脑读令牌后，执行器会话重建只撤销自身角色／代次；Run 结束撤销全部。避免 `_prime_spec` 当前全 Run revoke 误杀大脑读取能力。 |

### 问答交付的具体约定

独立判断在项目内部先完整保存，再决定原生运输形式。原 schema 校验移到原生映射边界，不能删除原生协议校验，也不能选择“最接近”的选项冒充答案。

既有 `ack_guidance` 表示收到／接受或提出异议，不表示执行完成。开放答案可以复用现有指导正文，不要为它再造一套消息系统。使用 request_id 关联一份答案及其交付，重复问题或迟到事件不能重复派发。

原生回执只支持封闭选择时：先保存答案，发送合法 decline/cancel，等已确认可投递的自然边界发送正文。确需等待这次决定的动作在既有 blocking/yield 机制中处理；不得产生“必须先确认才能送达”的死锁，也不能在根本没收到答案时按假定选择继续付费操作。研究问题的新共享入口迅速返回登记回执，不让 MCP 调用长时间同步等待大脑。

不是所有 native user-input 都是科研咨询。按能力和原有授权规则区分权限请求；任何不明确的授权不能由大脑代签。局部工具问题退回执行器自行处理的既有路径，不无差别进入长期研究会话。

### 轨迹读取的具体约定

只有大脑实际发出 list/read 时才读取内容。初始 packet 可给“本 Run 存在可读记录”的入口信息及已有简短状态；不能因为执行器提问或后台时钟自动展开日志。

范围先支持既有公开事件、检查点、Job 回执、提交／评分回执、可解析的冻结输入清单。用当前审阅的 run／frame／through_seq 约束服务端范围，模型不能自选其他 Run 或扩大到未来记录。允许查较早的 Trial，不能只查当前问题引用。按需加载已登记产物时，必须能确定版本；否则明确不可读。

读取仅检索本地已保存材料，不重新下载远端结果、不运行科学分析、不调用别的模型。内容先脱敏再返回；缺失与历史截断显式标记。记录实际提供的片段，不能称为读过整份文件。可采用设计中的有界输出默认值；超限不自动降级为整份报告注入。

通过现有原生 MCP 接线实现。尤其检查：`brains/kimi.py::open` 当前空 `mcpServers`、大脑进程环境对新令牌的传递、`codex_protocol.py` 工具 allowlist、默认拒绝请求的处理，以及 `_prime_spec()` 全 Run 撤销令牌的副作用。只修改本切片必需的接口；不假设外部 API 字段。

大脑只读工具例外与禁止复杂工具执行不冲突。不能“为了干净上下文”禁掉大脑选读；也不能“为了读取”给大脑通用 Shell。当前运行时若不能严格限制工具，记录真实能力边界，不切成普通聊天 API 或悄悄降级模型。

### 兼容与简化原则

优先使用现有 `review_requests`、frame/result JSON、guidance 和事件表，只在确实需要时追加少量字段。保留旧输出解析作为历史兼容，新会话使用开放协议。旧字段缺失不构造虚假高层结论；旧日志继续保留但不自动回灌。

文档、UI 状态和两个原生大脑适配都要对齐；不新增十几个中间阶段。已存在的 Job 门禁、unknown 记账、暂停恢复、模型身份与经验审批必须保持。写入失败、读取不足、原生通道无法表达自由答案，都应有可理解的状态，不能让整条研究循环悬挂。

## 3. 验收、交付与完成定义

### 最少需要的六组测试

| 验收组 | 场景与断言 |
|---|---|
| 开放答案 | 是／否问题返回第三路线，正文保存、投递、执行器实际消费；合法选择同样有效。验证旧非空 answers／const 校验不再阻止自由答案，原生回执仍符合 schema；长于 500 字符的有效理由不静默丢失。 |
| 可选读取 | 不读轨迹即可回答且读取数为零；另一条测试按需 list/read 到执行器没有引用的记录，返回绑定的来源与序号；无自动预读或强制核查。 |
| 证据与独立性 | 自述成功而回执失败／unknown时，读取接口忠实返回矛盾且不替模型裁决。提示中的诱导选项不变成硬约束；另含执行器判断正确的同意例子。 |
| 稀疏监督 | 正常长 Job 与重复轮询不唤醒；无求助的研究级变化／疑似停滞可以触发一次；大脑读取、token 用量、自己输出不造成循环唤醒。 |
| 上下文与权限 | 各入口都无默认完整日志；长期研究判断可延续。脑只读令牌不能写 Job／checkpoint；执行器重建不失效脑读令牌；其他 Run／未来记录拒绝。 |
| 交付与恢复 | native decline 不抹掉已有研究回答；没有长等待死锁或重复投递；暂停／结束后的迟到答案不驱动行动。应用回归、前端测试与构建通过或如实报告失败。 |

先使用 fake runtime＋临时工作区跑上述完整链路，并记录实际结果。已有测试可合并扩展，不要求机械新增固定数量测试文件。Fake 大脑输出证明运输机制，不证明真实模型独立性。

保留一个小型“同事实、不同诱导选项＋合理同意”的评估 fixture／运行入口即可。真实模型调用默认关闭；只有明确有界授权后运行。比较时固定模型、思考强度、资料和总预算，纳入读取后的上下文成本。禁止用“反对越多越好”“读日志越多越好”作为评价。没有真实调用时不得写“迎合已消除”。

### 应交付什么

完成实际代码、测试和研究页的最小展示。更新 `docs/DECISIONS.md` 与 `STATUS.md`，并提供一份短的 `docs/SPARSE_BRAIN_UPGRADE_RESULT.md`，写清：修改位置、执行过的命令及结果、开放答案与可选读取的一条实际回放、原生支持／未验证边界。

在文档中显式写出：**独立判断为核心，轨迹按需读取，未增加主动核查流程。** 对照说明原来哪些硬限制已解除，哪些只是协议提示调整。不要把已有 TBMA 修复重复计为新能力。

检查依据当前仓库选择命令，通常包括：

```bash
# 在项目已有虚拟环境和隔离测试数据下执行
.venv/bin/python -m pytest -q
(cd apps/web && npm test && npm run build)
git diff --check
```

代码完成后做一次有界审查，重点检查“自由回答在运输层再次被压回选择题”“没有请求就自动读轨迹”“只存库未交付”“读取令牌被误撤销”四类问题。无需另造审计系统。

### 什么时候算完成

一次实际执行的端到端应用测试（使用 fake runtime）必须证明：

> 执行器提出是／否问题；大脑可在零读取情况下给出第三种判断，也可自行选读一份执行器未推荐的已有记录；完整答案送达执行器；研究继续；正常工具活动没有把它变成持续审阅。

该测试应实际调用项目控制器、协议适配／测试桩与投递链，不能直接给预期变量赋值充当闭环。它仍然不等于真实模型或真实科研效果。

若存在原生版本限制，交付已经完成的源码和证据，准确列出尚未验证或被阻塞的那条链；不为了宣称完成而扩大权限、降级模型或伪造回执。不自动提交／推送 Git，除非用户另外授权。
