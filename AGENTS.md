# CyberScientist — 施工约定

## 目标与阅读顺序

在本工作区实现可用的单用户科学竞赛工作台。大脑用 Codex 或 Kimi Code 原生运行时；执行器用上游 Prime Agent；科学计算通过 bohr 在 Bohrium 执行。用户需要前端、可配置连接、可编辑经验和完整研究/提交闭环。

先读 `README.md`、`docs/BUILD.md`、`docs/ARCHITECTURE.md`。实现某个边界前再读对应的 `CONTRACTS.md`、`INTEGRATIONS.md`、`EXPERIENCE.md` 或 `FRONTEND.md`。不要先写另一套全面设计文档。

## 不可破坏的约束

| 约束 | 实施要求 |
|---|---|
| 最小垂直切片 | 一次做完整可验证流程；不建设通用多代理平台、插件市场、任务 DAG 或分布式调度 |
| 上游复用 | Prime 独立集成、固定版本；Codex/Kimi 使用原生会话和认证；不得重写代理内核或把 CLI 简化为普通聊天 API |
| Bohrium-first | 本地只编排、代码/文件/API/结果管理；科研依赖验证、科学计算、统计分析、科学作图均在 Bohrium Job 中完成。应用单元测试和前端构建可以本地执行 |
| 真实证据 | HTTP 成功、RPC 接受、上传完成、评分完成和参赛有效性分别确认；不知道就记录 unknown |
| 经验与证据分离 | 可编辑 Markdown；修订、失败和旧运行快照保留；Trace 不能直接晋升为有效经验 |
| 授权 | 开发不自动登录、不提交付费任务或比赛 Attempt。已有密钥不等于费用/提交授权；用户可在产品内给予有界的单轮授权 |
| 密钥 | 后端保存，前端写入后不回显；不写入 Git、经验、提示词、日志或提交包；不改用户全局 CLI 配置 |
| 进程可靠性 | 浏览器关闭不终止后台运行；后端重启先对账远程任务和提交，再决定恢复；不承诺恢复 IPython 内存 |

## 工作方式

先检查实际目录与已安装工具；目录非空时保留用户代码。可使用已安装的 Matt/Ponytail 技能，但不得假称已读不存在的 skill，也不为安装一套无关工具阻塞施工。

对外部协议先做最小探针，记录版本和结果，再接入主流程。一个改动验证一个假设。只增加当前切片必需的代码和测试。

协议不明时，隔离到对应适配器，完成其余可测试部分；禁止编造字段或把 fixture 当真实响应。凭据/授权缺失时输出准确缺项，继续完成可完成的代码，不停在空计划。

运行时内核文件、供应商协议和评分器不可在科研 Run 内自我修改。经验和 skill 候选可以提出，经过证据审查后用于下一轮。

## 完成与记录

`STATUS.md` 只写四类事实：已实现、已实际验证、尚未验证、阻塞项。记录执行过的命令与结果，不杜撰测试数字。每个阶段完成一次有界代码审查；已有可用代码不为迎合此设计全部重写。

仓库中 `prototype/` 是演示参考，不是生产前端。最终前端必须连接真实后端；Demo 模式必须始终标识且使用独立数据目录。

本包内部冲突时，以用户本轮明确要求、此文件的安全/证据边界和 `docs/DECISIONS.md` 为准；发现设计不适配实际 API，采用较小的修正并在 `DECISIONS.md` 追加原因和验证。
