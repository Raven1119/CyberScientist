# Kimi 单次施工任务：CyberScientist 非对称协作与静默监督

## 1. 目标与读取顺序

在**当前 CyberScientist 仓库**中，一次完成「执行器自主研究 → 大脑并行静默观察 → 必要时可靠介入 → 执行器确认与继续 → 证据和经验保留 → 前端可见」的最小生产切片。

大脑和执行器都可使用 Kimi Code / Codex。只有两个顶层代理；同一个大脑在静默审阅、被请求审阅和生命周期决策之间切换。不要恢复 Prime 为默认执行器，不要另建通用多代理框架。

先读当前 `AGENTS.md`，再按顺序读：

- [设计与范围](docs/collaboration/SPEC.md)
- [协议、交接和持久化](docs/collaboration/CONTRACTS.md)
- [代码审计参考](docs/collaboration/CODE_AUDIT.md)
- [验收](docs/collaboration/ACCEPTANCE.md)

提示词片段位于 `prompts/collaboration/`，消息样例和 Schema 位于 `docs/collaboration/`。这些文件描述目标行为；现有代码决定最小修改路径。审计参考基于远端 `main@27609e6afcc4f9987ec8459e87ff365b650d790c`，不得假定与当前工作树完全相同。

## 2. 在本任务中完成审计、实现与验证

**先核实代码，随即施工。** 检查 `git status`、HEAD、现有指令和测试；沿真实启动链追踪控制器、运行时、检查点、事件、经验和前端。把“已存在 / 缺口 / 最小修改”写进 `docs/collaboration/IMPLEMENTATION_RESULT.md` 的审计部分；然后继续实现，不停在计划或审计报告，不等待下一张任务卡。

**完成一条完整闭环。** 复用当前 FastAPI / SQLite / React / JSON-RPC / 原生会话。优先修复阻塞事件循环、回合结束误判实验完成和指导无法排队三个集成问题。接通被动观察帧、单飞大脑调度、静默结果、持久化指导队列、执行器主动请求、确认与恢复；把真实状态接入现有研究工作台。复用经验编辑和修订，确保代理真正读到选中修订的正文。

在当前已有的 Kimi 和 Codex 适配器上实现共同的边界投递语义；不以“两种运行时”为理由复制控制器。首版不依赖回合内 steer。若某原生能力缺失，提供明确能力状态和边界投递，不假装已送达。保持现有授权、Bohrium-first、密钥和数据保留边界，不重写不相关的平台模块。

**用证据结束任务。** 先补能暴露问题的测试，再实现，运行相关回归、已有全套测试和前端构建；执行 `ACCEPTANCE.md` 的交错事件回放和浏览器路径。最后做一次有界代码审查，修复发现的阻断问题并复测。只有已有凭据且已有明确费用授权时才做有界真实探针；没有授权时完成实现、假协议集成测试和可执行探针入口，明确标记真实联调未验证。

## 3. 交付条件与边界

交付可运行代码、最小数据库迁移、真实后端驱动的 UI、回归测试、实际执行命令与结果、能力矩阵及准确阻塞项。更新根 `AGENTS.md` 中过期的 Prime 主路线、`STATUS.md` 的当前状态摘要，以及必要的现行文档；保留历史记录。实现中的必要设计修正记在交付报告中，不另开规划项目。

本轮不增加第三个摘要代理、代理群、知识图谱、向量库、调度服务、科学策略 DSL 或自动 harness 进化；不做全仓改名和全量重构。不要把脚本化 Demo 通过算作真实模型或科研得分通过。

保留用户未提交改动、历史失败和经验版本；不得擅自 reset、删除数据、修改全局 CLI 配置、创建付费科研任务、提交比赛、commit 或 push。开发测试使用隔离目录。

最终汇报仅给：实际完成内容、验证证据、尚未验证/阻塞项。**现在开始审计当前代码，并在同一次任务中完成实现、修复和验证。**
